%!TeX program = xelatex
\documentclass{SYSUReport}
\usepackage{amsmath}
\usepackage{tikz}
\usepackage{tikz-qtree}
\usepackage{adjustbox} % 加入调整表格大小的宏包
\usepackage{booktabs} % 用于创建更漂亮的表格
\usepackage{multirow} % 用于合并单元格
\usepackage{graphicx}
\usepackage{longtable}
\usepackage{amsmath}

\usepackage{listings}
\usepackage{xcolor}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{gray!10},      % 背景色
    basicstyle=\ttfamily\small,           % 基本字体
    keywordstyle=\color{blue!70!black},   % 关键字颜色
    commentstyle=\color{green!40!black},  % 注释颜色
    stringstyle=\color{purple!60!black},  % 字符串颜色
    numbers=left,                         % 行号
    numberstyle=\tiny\color{gray},
    frame=single,                         % 边框
    breaklines=true,                      % 自动换行
    captionpos=b                          % caption 在底部
}

\lstset{style=mystyle}

% 根据个人情况修改
\headl{23352078 赵一鸣}
\headr{数据安全和隐私保护}
\lessonTitle{数据安全和隐私保护}
\reportTitle{数据安全和隐私保护第1次实验}
\stuname{赵一鸣}
\stuid{23352078}
\inst{网络空间安全学院}
\major{网络空间安全}
% 修改为具体日期
\date{2026 年 9 月 10 日}

\begin{document}

\cover
\thispagestyle{empty} % 首页不显示页码
\clearpage

\tableofcontents
\clearpage

\pagenumbering{arabic}
\setcounter{page}{1}

\section{实验要求}

本次实验（实验一：基础工具使用）的要求如下：

\begin{enumerate}
  \item 了解 LaTeX 中图片插入的基本方法，在实验报告中实际插入至少一张实验结果图；
  尽量使用 PDF、SVG、EPS 等矢量图片，避免低分辨率截图；掌握
  \texttt{\textbackslash includegraphics} 的基本使用并合理设置图片大小；
  图片应包含规范的图编号、图标题，并在正文中引用。
  \item 掌握 Git 和 GitHub 的基本使用方法：安装并配置 Git，在 GitHub 建立个人实验代码仓库，
  完成仓库的创建、克隆（初始化）、提交、上传和更新；完成两次本地提交，
  在 GitHub 网页修改 README 后用 \texttt{git pull} 同步，并用 \texttt{git log} 核对。
  \item 完成 Codex 的安装和基本配置，下载并配置 cc-switch，对供应商进行配置与切换，
  并使用 Codex 完成至少一次任务（代码阅读、生成或修改）。
  \item 实验报告需包含必要的操作步骤、关键命令、过程截图、代码修改情况及实验总结，
  尽量使用 LaTeX 模板编写。
\end{enumerate}

\section{实验原理}

\subsection{Git 版本控制模型}

Git 是分布式版本控制系统。工作区保存正在编辑的文件；\texttt{git add}
把修改送入暂存区；\texttt{git commit} 把暂存内容记录为本地仓库中的一个版本；
\texttt{git push} 把本地提交上传到 GitHub 等远程仓库，\texttt{git pull}
则把远程更新拉取并合并到本地。提交前用 \texttt{git status} 判断哪些文件发生了变化，
用 \texttt{git diff} 与 \texttt{git diff -{}-staged} 分别查看未暂存和已暂存的差异，
用 \texttt{git log} 追溯提交历史。推荐的固定节奏为：
开始工作先 \texttt{pull}，修改后查看 \texttt{status} 和 \texttt{diff}，
确认后 \texttt{add}、\texttt{commit}，结束工作再 \texttt{push}。

\subsection{Codex 辅助编程与 cc-switch}

Codex 是命令行 AI 编程助手，能在用户授权范围内读取项目、修改文件并运行本地工具。
它根据自然语言需求和仓库上下文制定并执行操作，但生成结果可能存在错误或不安
全修改，因此可靠流程应包含：权限确认、命令审阅、程序运行、测试和 Git 差异检查；
Codex 负责辅助理解与修改，Git 负责保存可核对的版本。

cc-switch 是第三方开源桌面工具，把不同供应商的端点、模型和认证配置保存为可切换
条目，通过更新 Codex 的配置文件（\texttt{\textasciitilde/.codex/config.toml}）
实现切换；切换后需重启 Codex 使配置生效。使用第三方供应商前应了解其计费、
数据处理和账号规则，且注意不要把 API Key 写入报告、截图或仓库。

\subsection{LaTeX 插图与矢量图}

位图由像素组成，放大后会出现锯齿或模糊；矢量图保存曲线、文字和几何对象，
缩放时仍能保持清晰，因此折线图、柱状图等科研图表应优先导出为 PDF/SVG 矢量格式。
LaTeX 通过 \texttt{graphicx} 宏包的 \texttt{\textbackslash includegraphics}
把图片按版面尺寸排入文档（用 \texttt{width=0.8\textbackslash textwidth}
控制宽度），\texttt{\textbackslash caption} 生成图编号与图题，
\texttt{\textbackslash label} 与 \texttt{\textbackslash ref} 建立正文引用。

\section{实验设置}

\begin{table}[htbp]
  \centering
  \caption{实验环境与工具版本}
  \label{tab:env}
  \begin{tabular}{ll}
    \toprule
    项目 & 版本/说明 \\
    \midrule
    操作系统 & Windows 11 + WSL2 (Ubuntu 22.04) \\
    Git & 2.34.1（Ubuntu 软件源预装） \\
    Python / matplotlib & 3.10.12 / 3.10.9 \\
    Node.js / Codex CLI & v22.9.0 / codex-cli 0.154.0 \\
    cc-switch & 3.20.1（Windows GUI 版） \\
    供应商 / 模型 & 智谱 Zhipu GLM（open.bigmodel.cn）/ glm-5.3 \\
    代码仓库 & \texttt{github.com/Zhaoym53/research-tools-week1} \\
    \bottomrule
  \end{tabular}
\end{table}

实验代码托管于本仓库：词频统计程序 \texttt{code/text\_stats.py}，
绘图脚本 \texttt{code/plot\_wordfreq.py}，示例文本 \texttt{sample.txt}，
运行结果（含矢量结果图）位于 \texttt{result/} 目录，LaTeX 报告位于 \texttt{report/} 目录。

\section{实验过程}

\subsection{Git 的安装与配置}

WSL2 Ubuntu 环境已通过软件源预装 Git，执行 \texttt{git -{}-version} 确认版本为
2.34.1。随后配置提交记录中显示的姓名和邮箱（写入提交历史，不等同于 GitHub 登录密码）：

\begin{lstlisting}[language=bash,caption={Git 安装确认与身份配置}]
git --version
git config --global user.name "Zhaoym53"
git config --global user.email "13361470120@163.com"
git config --global init.defaultBranch main
git config --global --list --show-origin
\end{lstlisting}

本环境已配置 GitHub 的 SSH 密钥（\texttt{\textasciitilde/.ssh/id\_ed25519}），
用 \texttt{ssh -T git@github.com} 验证通过，后续与远程仓库的收发均走 SSH 认证，
避免在命令或配置文件中出现密码和令牌。

\subsection{本地初始化与两次提交}

先在本地初始化仓库并按实验要求建立 \texttt{code/}、\texttt{result/}、
\texttt{report/}、\texttt{figures/} 四类目录，然后编写词频统计程序：

\begin{lstlisting}[language=bash,caption={仓库初始化}]
git init -b main
mkdir -p code result report figures
\end{lstlisting}

第一次提交加入 README 和一个可运行的小程序 \texttt{code/text\_stats.py}
（统计英文文本词频，支持 \texttt{-n} 指定显示条数、\texttt{-{}-json} 导出完整结果）。
提交前先用 \texttt{status} 和 \texttt{diff} 核对变化，再明确列出要暂存的文件：

\begin{lstlisting}[language=bash,caption={第一次提交：程序与说明}]
git status
git diff
git add README.md .gitignore code/text_stats.py sample.txt
git commit -m "add README and word frequency counter program"
\end{lstlisting}

运行程序验证输出正确后，把运行结果（\texttt{result/sample\_stats.txt} 与
\texttt{result/sample\_stats.json}）、绘图脚本及其生成的矢量图作为第二次提交。
两次提交各自只完成一个清晰目标，提交说明使用动词描述本次变化：

\begin{lstlisting}[language=bash,caption={运行程序与第二次提交}]
python3 code/text_stats.py sample.txt -n 15 --json result/sample_stats.json
python3 code/plot_wordfreq.py
git add code/plot_wordfreq.py result/
git commit -m "add run results and vector word frequency chart"
\end{lstlisting}

程序输出如下（共 165 词，其中 data 出现 10 次、privacy 出现 9 次）：

\begin{lstlisting}[caption={text\_stats.py 运行输出（节选）}]
文件: sample.txt
总词数: 165，不同单词数: 111，前 5 名:
排名    单词            次数    占比
1      data           10     6.1%
2      privacy        9      5.5%
3      and            8      4.8%
4      protection     5      3.0%
5      the            5      3.0%
\end{lstlisting}

用简洁历史核对两次本地提交：

\begin{lstlisting}[caption={git log 核对提交历史}]
$ git log --oneline --graph --decorate -n 10
* c079d2b (HEAD -> main) add run results and vector word frequency chart
* 541e152 add README and word frequency counter program
\end{lstlisting}

此时本地历史共两次提交；后续上传远程、网页修改与再同步后的完整历史
如图~\ref{fig:gitlog}~所示。

\subsection{创建 GitHub 仓库并上传}

在 GitHub 网页新建空仓库 \texttt{research-tools-week1} 后，把本地 main 分支
与其建立关联并首次上传（\texttt{-u} 建立上游关系，以后直接 \texttt{git push}）：

\begin{lstlisting}[language=bash,caption={关联远程仓库并首次上传}]
git remote add origin git@github.com:Zhaoym53/research-tools-week1.git
git push -u origin main
\end{lstlisting}

上传成功后，GitHub 仓库首页已能看到 README、code、report、result 等目录
与提交记录，如图~\ref{fig:githubrepo}~所示。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.9\textwidth]{github_repo.png}
  \caption{GitHub 仓库首页：目录与提交记录均已上传}
  \label{fig:githubrepo}
\end{figure}

\subsection{远程更新与 git pull 同步}

在 GitHub 网页上直接修改 README 的一行文字（把任务清单第一项打勾，
提交说明为 "Mark tasks as completed in README"）后，本地先确认工作区干净，
再执行 \texttt{git pull} 取得远程提交并以 Fast-forward 方式合并；
最后用 \texttt{git log} 核对"多次本地提交 + 一次远程更新"的历史，
如图~\ref{fig:gitlog}~所示：

\begin{lstlisting}[language=bash,caption={拉取远程更新并核对}]
git status        # nothing to commit, working tree clean
git pull          # Fast-forward, README.md | 6 +++---
git log --oneline --graph --decorate -n 10
\end{lstlisting}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\textwidth]{git_log.png}
  \caption{首次 push、网页修改 README 后 git pull 同步并用 git log 核对历史}
  \label{fig:gitlog}
\end{figure}

从图~\ref{fig:gitlog}~可以看到完整的历史链：
三次本地提交（\texttt{541e152} $\rightarrow$ \texttt{c079d2b} $\rightarrow$
\texttt{f7522d8}）与一次网页端远程更新（\texttt{180ed92}），
\texttt{git pull} 以 Fast-forward 合并、无冲突，\texttt{git status}
在拉取前后均保持 working tree clean，操作顺序规范。

\subsection{Codex 的安装、配置与辅助编程}

\subsubsection{安装 Codex CLI}

本环境 chatgpt.com 的官方安装脚本不可直接访问，改用 npm 镜像源安装：
先在用户目录安装 Node.js v22.9.0（无需管理员权限），再全局安装 Codex CLI，
安装后确认版本：

\begin{lstlisting}[language=bash,caption={npm 安装 Codex CLI}]
npm install -g @openai/codex --registry=https://registry.npmmirror.com
codex --version   # 输出: codex-cli 0.154.0
\end{lstlisting}

\subsubsection{cc-switch 配置供应商并验证切换}

本机网络环境无法直连 ChatGPT 登录，因此采用 cc-switch 配置第三方供应商的方案。
在 Windows 上安装 cc-switch v3.20.1，在 Codex 页面添加智谱 Zhipu GLM 供应商
（Endpoint 为 \texttt{open.bigmodel.cn}，模型 \texttt{glm-5.3}，API Key 填入
供应商配置中），保存并点击卡片上的"启用"，如图~\ref{fig:ccswitch}~所示。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.9\textwidth]{ccswitch.png}
  \caption{cc-switch 供应商管理界面：Zhipu GLM 已启用（API Key 未显示）}
  \label{fig:ccswitch}
\end{figure}

由于本实验的 Codex CLI 安装在 WSL 内，而 cc-switch 改写的是 Windows 侧用户目录
下的 \texttt{\textasciitilde/.codex/} 配置，因此每次切换供应商后需把配置文件
同步到 WSL 侧并重开终端生效。这里踩过一个坑：第一次只同步了
\texttt{config.toml} 而漏掉 \texttt{cc-switch-model-catalog.json}，
Codex 启动即报 \texttt{Error loading configuration: No such file or directory}；
把配置涉及的文件完整同步后恢复正常。为方便起见在 \texttt{\textasciitilde/.bashrc}
中定义了同步别名：

\begin{lstlisting}[language=bash,caption={把 cc-switch 生成的配置同步到 WSL 侧}]
alias synccodex='cp /mnt/c/Users/<Windows用户名>/.codex/config.toml \
  /mnt/c/Users/<Windows用户名>/.codex/cc-switch-model-catalog.json ~/.codex/'
synccodex && exec bash
\end{lstlisting}

重开终端进入仓库运行 \texttt{codex}，用 \texttt{/status} 验证：
当前模型为 \texttt{glm-5.3}（reasoning high）、供应商为 \texttt{custom}、
权限为工作区写入 + 命令审批（Workspace, Ask for approval），
说明供应商切换已生效，如图~\ref{fig:codexstatus}~所示。
API Key 只保存在本地配置文件中，本报告所有截图均已遮挡密钥信息。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.9\textwidth]{codex_status.png}
  \caption{/status 验证：模型已切换为 glm-5.3，供应商为 custom}
  \label{fig:codexstatus}
\end{figure}

\subsubsection{使用 Codex 完成辅助编程任务}

\textbf{任务一（代码阅读）}：进入仓库目录运行 \texttt{codex}，要求它只阅读项目、
说明目录结构、程序入口和运行方法而不修改文件：

\begin{lstlisting}[language=bash,caption={任务一：只读阅读项目}]
codex "请先阅读项目，说明目录结构、程序入口和运行方法，不要修改文件。"
\end{lstlisting}

Codex 依次执行 \texttt{pwd}、\texttt{ls}、\texttt{find} 等只读命令浏览目录，
阅读 README.md、\texttt{text\_stats.py}、\texttt{plot\_wordfreq.py} 与
\texttt{main.tex}，随后正确总结出目录结构、程序入口（两个脚本的
\texttt{main()} 与命令行参数）和运行方法，全程未修改任何文件，
如图~\ref{fig:codexread}~所示。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\textwidth]{codex_read.png}
  \caption{任务一：Codex 只读浏览项目并总结结构与运行方法}
  \label{fig:codexread}
\end{figure}

\textbf{任务二（代码修改）}：要求 Codex 给词频统计程序增加可选的英文停用词
过滤功能，并说明测试方法：

\begin{lstlisting}[language=bash,caption={任务二：修改程序并说明测试方法}]
codex "请给 code/text_stats.py 增加可选的英文停用词过滤功能，
      并说明如何测试。"
\end{lstlisting}

Codex 给出修改方案：新增内置停用词集合 \texttt{ENGLISH\_STOPWORDS} 与
\texttt{-{}-stopwords} 命令行开关，默认不启用、保持原行为不变，并附上测试命令
与预期输出，如图~\ref{fig:codexdiff}~所示。审阅其方案与命令后批准执行，
退出 Codex 后用 Git 独立检查修改：

\begin{lstlisting}[language=bash,caption={退出 Codex 后独立检查差异并运行验证}]
git status        # 仅 M code/text_stats.py，无其他改动
git diff          # 逐行审查：新增停用词集合、--stopwords 开关、计数逻辑
python3 code/text_stats.py sample.txt -n 5
python3 code/text_stats.py sample.txt -n 5 --stopwords
\end{lstlisting}

独立运行的输出与 Codex 给出的测试预期完全一致：
默认行为不变（165 词，data/privacy/and 居前）；开启 \texttt{-{}-stopwords}
后总词数降为 113、不同单词数 82，and/the/of 等虚词被过滤，
前 5 名全部为主题词：

\begin{lstlisting}[caption={修改后程序独立验证输出（--stopwords 开启）}]
文件: sample.txt
总词数: 113，不同单词数: 82，前 5 名，已过滤内置英文停用词:
排名    单词            次数    占比
1      data           10     8.8%
2      privacy        9      8.0%
3      protection     5      4.4%
4      encryption     4      3.5%
5      security       3      2.7%
\end{lstlisting}

确认无误后提交并推送到 GitHub：

\begin{lstlisting}[language=bash,caption={提交 Codex 的修改}]
git add code/text_stats.py
git commit -m "add optional stopword filtering to text_stats"
git push
\end{lstlisting}

\subsection{矢量结果图的生成与 LaTeX 排版}

词频统计的 JSON 结果由 \texttt{code/plot\_wordfreq.py} 读入，
用 matplotlib 绘制水平柱状图并直接导出为 PDF 矢量图
（\texttt{fig.savefig("result/wordfreq.pdf")}）。矢量图放大不失真，
避免了截图反复采样造成的清晰度损失。图片统一放入 \texttt{figures} 目录，
文件名使用简短英文；插入时用 \texttt{graphicx} 宏包并合理设置宽度，
通过 \texttt{\textbackslash caption} 生成图题、\texttt{\textbackslash label}
与正文 \texttt{\textbackslash ref} 建立引用。
本报告的实验结果图如图~\ref{fig:wordfreq}~所示，
正文中的"图~\ref{fig:wordfreq}"即为编译后自动生成的编号引用。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.80\textwidth]{wordfreq.pdf}
  \caption{sample.txt 词频统计结果（出现频次前 10 的单词）}
  \label{fig:wordfreq}
\end{figure}

从图~\ref{fig:wordfreq}~可以看出，语料中 data（10 次）、privacy（9 次）、
encryption（4 次）等主题词频率最高，与文本"数据安全与隐私保护"的主题一致；
and、the 等虚词虽然也高频出现，但不携带主题信息。对比 4.5 节开启
\texttt{-{}-stopwords} 后的输出，data 的占比从 6.1\% 升至 8.8\%，
主题词更加突出，这也说明了停用词过滤在文本统计中的必要性。
将结果图放大观察，文字与坐标轴均保持清晰，验证了矢量图相对截图的优势。

\section{实验总结}

通过本次实验，我搭建了一条"本地开发—版本管理—远程备份—AI 辅助—报告撰写"
的完整科研工具链，主要收获如下：

\begin{enumerate}
  \item \textbf{Git 方面}：理解了工作区、暂存区、本地仓库、远程仓库的四层模型，
  养成了"先 pull、改后 status/diff、确认后 add/commit、结束 push"的固定节奏。
  明确列出暂存文件、一次提交只做一个目标、提交说明用动词，这些规范让
  \texttt{git log} 中的每条历史都有清晰的含义；网页修改与本地 \texttt{pull}
  的 Fast-forward 合并也让我直观理解了"远程更新"的同步过程。
  \item \textbf{AI 辅助编程方面}：Codex 能快速读懂项目结构并给出可用的修改
  （停用词过滤功能的测试预期与我的独立运行结果完全一致），但它的产出必须经过
  人工审查——先阅读它准备执行的命令，再运行测试验证，最后用 \texttt{git diff}
  独立检查每一处改动后才提交。AI 负责效率，Git 负责可核对的版本，二者缺一不可。
  \item \textbf{LaTeX 与矢量图方面}：体会到矢量图（PDF）相对截图的优势——
  缩放不失真、可重复生成；掌握了 \texttt{\textbackslash includegraphics}、
  \texttt{\textbackslash caption}、\texttt{\textbackslash label/\textbackslash ref}
  的规范用法，为后续实验报告的撰写打下了基础。
  \item \textbf{排错与安全意识方面}：cc-switch 在 Windows 侧改写配置，而 WSL 内的
  Codex 读取的是 Linux 侧文件，只同步一半配置文件就会导致启动失败——
  跨子系统搭建工具链时必须清楚"每一份配置到底存在哪里"。整个过程中 API Key
  只存在于 cc-switch 的本地配置中，未出现在任何命令输出、截图或仓库文件里；
  SSH 密钥认证也避免了在命令行中暴露凭据。
\end{enumerate}

后续可以改进的方向：使用分支开展实验性修改，熟练处理合并冲突；
给 \texttt{text\_stats.py} 补充单元测试；把报告编译集成到仓库的持续集成中。

% 如需参考文献可以取消以下两行代码注释并在reference.bib中添加bib引用，然后在正文中用\ref引用
% \bibliographystyle{ieeetr}   % 或 plain / unsrt / IEEEtran 等
% \bibliography{reference}

\end{document}
